# Nombre de la aplicación
spring.application.name=backend

# Conexión a la base de datos (se toma de variable de entorno)
spring.datasource.url=${MYSQL_ADDON_URI}
spring.datasource.username=${MYSQL_ADDON_USER}
spring.datasource.password=${MYSQL_ADDON_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# Logging
logging.level.org.hibernate.SQL=debug
logging.level.org.springframework.web=DEBUG

# Puerto del servidor
server.port=8081

# Configuración para envío de correos (para recuperación de cuenta u otras notificaciones)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=stivencante7@gmail.com
spring.mail.password=ngkl whba ecap gykx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Límite de tamaño para archivos subidos
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB
